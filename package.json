{
  "name": "perler-beads-generator",
  "version": "0.1.0",
  "description": "A powerful Perler bead pattern editor for turning images into printable bead charts with MARD color matching, layers, 3D preview, and usage exports.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build && node scripts/dev-server.cjs",
    "build": "tsc && node scripts/clean-dist.cjs && tsc -p tsconfig.build.json && node scripts/post-build.cjs && node scripts/write-html.cjs",
    "preview": "node scripts/dev-server.cjs"
  },
  "dependencies": {
    "@types/three": "^0.184.1",
    "@vitejs/plugin-react": "^4.2.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "three": "^0.184.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3"
  }
}
